{{define "home" -}}
{{- $conv := .Conversions -}}
<!DOCTYPE html>
<html lang="en">
{{ template "html-head" headData .CommonPageData "Monetarium Block Explorer"}}
    {{ template "navbar" . }}
    <div class="container main" data-controller="time blocklist homepage mining distribution voting"
         data-action="homepage:block->mining#handleBlock homepage:block->distribution#handleBlock homepage:block->voting#handleBlock">
        <div class="row">
            <div class="d-flex flex-column col-md-15 p-0">
                <div class="bg-white mb-1 py-2 px-3 my-0">
                    <div class="d-inline-block position-relative p-2">
                        <span class="card-icon dcricon-twoblocks h1 me-2"></span> <a href="/blocks" class="h3 my-3">Latest {{.NetName}} Blocks</a>
                    </div>

                    <div class="row">
                        <div class="col">
                            {{template "home_latest_blocks" .}}
                            <a href="/blocks" class="small ms-2">more blocks...</a>
                        </div>
                    </div>
                </div> <!-- end blocks card -->

                {{template "mempoolCard" .}}
            </div> <!-- end column -->

            <div class="d-flex flex-column col-md-9 p-0">
                {{template "voting-card" .}}

                {{template "mining-card" .}}

                {{with .Info}}
                <div class="bg-white mb-1 p-2 px-3 mx-1 flex-grow-1">
                    <div class="my-3 h4">
                        <span class="dcricon-tree d-inline-block pe-2 h4"></span>
                        Supply
                    </div>
                    <div class="row mt-1">
                        {{if .VARCoinSupply}}
                        <div class="col-12 mb-3 mb-sm-2 mb-md-3 mb-lg-3">
                            <div class="fs13 text-secondary">VAR Circulating Supply</div>
                            <div class="mono lh1rem fs14-decimal fs24 p03rem0 d-flex align-items-baseline">
                                <span data-distribution-target="varCirculating">
                                    {{formatCoinAtoms .VARCoinSupply.Circulating 0}}
                                </span>
                                <span class="ps-1 unit lh15rem">VAR</span>
                            </div>
                            <div class="fs12 lh1rem text-black-50">
                                Target: {{formatCoinAtoms .VARCoinSupply.Target 0}} VAR
                            </div>
                        </div>
                        {{end}}
                        {{if .SKACoinSupply}}
                        <div class="col-12 mb-3 mb-sm-2 mb-md-3 mb-lg-3">
                            <div class="fs13 text-secondary">SKA Supply</div>
                            {{range .SKACoinSupply}}
                            <div class="fs12 lh1rem">
                                <span class="text-secondary">SKA-{{.CoinType}}:</span>
                                InCirc: {{formatCoinAtoms .InCirculation .CoinType}} | Issued: {{formatCoinAtoms .TotalIssued .CoinType}} | Burned: {{formatCoinAtoms .TotalBurned .CoinType}}
                            </div>
                            {{end}}
                        </div>
                        {{end}}
                        {{if $conv -}}
                        <div class="col-12 mb-3 mb-sm-2 mb-md-3 mb-lg-3">
                            <div class="fs13 text-secondary">
                                <a class="no-underline" href="/market">Exchange Rate</a>
                            </div>
                            <div class="mono lh1rem fs14-decimal fs24 p03rem0 d-flex align-items-baseline">
                                <span class="d-inline-block" data-distribution-target="exchangeRate">{{$conv.ExchangeRate.TwoDecimals}}</span>
                                <span class="ps-1 unit lh15rem">{{$conv.ExchangeRate.Index}}</span>
                            </div>
                        </div>
                        {{- end}}
                    </div>
                </div>
                {{- end}}
            </div> <!-- end column -->
        </div>
    </div>
    <!-- end wrapper -->
    {{  template "footer" . }}
<script>
function mnToggleCoinRows(row) {
  var next = row.nextElementSibling;
  if (next && next.classList.contains('mn-coin-rows')) {
    next.style.display = next.style.display === 'none' ? '' : 'none';
  }
}
</script>
</body>
</html>
{{- end}}
